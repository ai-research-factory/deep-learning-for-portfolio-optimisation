[project]
name = "deep-learning-for-portfolio-optimisation"
version = "0.1.0"
description = "Deep Learning for Portfolio Optimisation"
requires-python = ">=3.10"
dependencies = ["numpy", "pandas", "scikit-learn", "torch", "matplotlib", "tqdm"]
